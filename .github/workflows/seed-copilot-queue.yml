# Crea issues listos para el Copilot coding agent (cola versionada en agent-queue/tasks.json).
# Ejecutar: Actions → Seed Copilot task queue → elegir ámbito → Run workflow.
# Idempotencia: no duplica si ya existe un issue abierto con el mismo <!-- agent-task-id:... -->.
name: Seed Copilot task queue

on:
  workflow_dispatch:
    inputs:
      scope:
        description: "Ámbito de tareas a generar"
        required: true
        default: all
        type: choice
        options:
          - all
          - homepage
          - theme
          - content_seo
          - security
          - commerce
          - docs

permissions:
  contents: read
  issues: write

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create issues from queue (deduplicado)
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = '.github/agent-queue/tasks.json';
            const raw = fs.readFileSync(path, 'utf8');
            const { tasks } = JSON.parse(raw);
            const scope = core.getInput('scope') || 'all';
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            const filtered = tasks.filter(
              (t) => scope === 'all' || t.scope === scope
            );
            core.info(`Scope=${scope} → ${filtered.length} tareas candidatas`);

            let created = 0;
            let skipped = 0;

            for (const task of filtered) {
              const marker = `agent-task-id:${task.id}`;
              const q = `repo:${owner}/${repo} is:issue is:open "${marker}" in:body`;
              const { data: search } = await github.rest.search.issuesAndPullRequests({
                q,
                per_page: 1,
              });
              if (search.total_count > 0) {
                core.info(`Skip (ya existe abierto): ${task.id}`);
                skipped++;
                continue;
              }

              const footer =
                '\n\n---\n**Siguiente paso:** en la barra lateral del issue, asigna **GitHub Copilot** (coding agent) para que abra un PR, o trabaja el issue manualmente. Ver [guía de cola](https://github.com/' +
                owner +
                '/' +
                repo +
                '/blob/main/.github/COPILOT-AGENT-QUEUE.md).';

              await github.rest.issues.create({
                owner,
                repo,
                title: task.title,
                body: task.body + footer,
                labels: task.labels,
              });
              core.info(`Creado: ${task.id}`);
              created++;
            }

            core.info(
              `Cola Copilot: creados=${created} omitidos=${skipped} scope=${scope}`
            );
