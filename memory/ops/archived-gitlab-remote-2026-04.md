# GitLab — archivo (no remoto activo)

**Estado (abril 2026):** el equipo no usa GitLab en el flujo diario. La fuente de verdad del código es **GitHub** (`Gano-digital/Pilot`).

**Referencia histórica** (por si hiciera falta recuperar algo en GitLab.com):

- Proyecto: `https://gitlab.com/Ouroboros1984/Gano.digital`

En clones locales antiguos puede existir un remoto `gitlab`; conviene ejecutar `git remote remove gitlab` y dejar solo `origin` apuntando a Pilot. Ver `.github/DEV-COORDINATION.md`.
